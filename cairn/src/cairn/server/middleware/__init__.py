"""Server middleware package.

Additive module introduced by the Cairn product UI work. Holds the
authentication dependency used to protect existing routers without modifying
them (see :mod:`cairn.server.middleware.auth`).
"""
